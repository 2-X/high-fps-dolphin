$DuneBud emitter null-guard BSE (skip stores when JPA 0x55 missing)
* "Invalid write to 0x00000154/158, PC=0x801d2bcc/d0" on EVERY Dune Bud
* (SandBombBase) spray, BSMSO/BSE disc only. Root cause: VANILLA code
* (byte-identical in pristine + BSMSO main.dol) creates the sand-dust JPA
* emitter (resource 0x55) at 0x801d2bc0 (bl -> 0x8028856c) and stores six
* scale fields (+0x154..+0x17c) through the returned pointer with NO null
* check. The BSMSO repacked particle set fails to provide 0x55 -> NULL ->
* null writes (benign under Dolphin, would crash real HW). Offline/pristine
* disc unaffected.
* Guard: cmpwi r3,0 at the first store; non-null -> original stfs, resume;
* null -> bctr to 0x801d2be4, which reloads r3 (lwz gpMarDirector) — the
* whole store block is skipped safely. The dust effect itself remains
* missing on the BSE disc until resource 0x55 is restored to the archive
* (see bsmso/mac-online/dunebud_watch.py for the registration-path probe).
C21D2BCC 00000005
2C030000 4182000C
D0030154 48000018
3D80801D 618C2BE4
7D8903A6 4E800420
60000000 00000000
