---
name: sunshine-portal-glow-bug
description: "SOLVED 2026-07-31: SMS rainbow-M portals now work at full 120fps normal speed. $120fps + TRUE-FIX v2 Gecko (FX emitter fix + ForceOpen + reimplemented proximity glow, ~354u radius). User: 'works amazingly, stunned.' Full root-cause history inside."
metadata: 
  node_type: memory
  type: project
  originSessionId: 764823c6-5696-4125-89c5-057d6eca32c4
---

**Symptom (confirmed by user 2026-07-30):** With the high-fps hack ON (60fps AND 120fps), the rainbow-M `TModelGate` stage portals in Delfino Plaza stay permanently "dark" and don't brighten as Mario approaches, so you can't jump in. At native 30fps (hack off, EmulationSpeed=1.0, stock Dolphin) they glow-on-approach and work normally. **Not a save-file problem**: decoded the memcard save (`01-GMSE-...gci`), story flag `0x10386` (Ricco gate cleared) is set and shine count is 4; game state is healthy.

**Disasm ground truth (USA `work/main.dol`, capstone in `/tmp/smsvenv`):**
- The `$120FPS` hack = 3 independent behavioral parts + 1 render-throughput part:
  - **A (timestep scale):** `044167B8 40000000` writes 2.0 to `0x804167B8` (stock 0.5 = fps/60; r2 base = `0x8041E790`, so it's `-0x7fd8(r2)`). Plus C2 hook `C20066EC` injects `lfs f22,-0x7fd8(r2); fmuls f1,f1,f22; fmr f22,f1` at `0x800066EC`, **inside a per-frame effect/particle sim loop at `0x80005F68`** (0x50-byte stride, squared-distance math). Stock did plain `fmr f22,f1` (unscaled); hack multiplies a per-entry value by 2.0.
  - **B (const):** `04414904 3CA3D70A` writes 0.02 to `0x80414904` (stock 0.01).
  - **C (throughput):** `042FCB24 60000000` NOPs `bl 0x8034f684`, one of FOUR retrace-waits in the VI pacing loop `0x802FC9AC` (`0x8034f684` = OSDisableInterrupts + sleep-until-framecount-advances + restore). Pure render throughput. **This NOP is an `04` code = always on at any target fps** → explains why 60 and 120 both break the gate identically while A's magnitude differs.

**Round-1 bisection results (user, 2026-07-30):** leave-one-out `$TEST noA/noB/noC`.
- noA (value stays 0.5, no fmuls; NOP+const0.02 on): **double-speed** physics, normal audio. M still broken.
- noB (const reverts to stock 0.01; value2.0+NOP+fmuls on): **correct speed AND fixed the double-speed menu/camera animations** that full $120FPS had. M still broken. → **new best baseline.** So const `0x80414904` 0.01→0.02 (part B) was what sped up menus/camera; drop it.
- noC (no NOP; value2.0+const0.02+fmuls on): **half-speed**, normal audio. M still broken.
- **Conclusion: no single Gecko component fixes the M** → round-1 was confounded because I bundled the value-write (A) with the effect-loop `fmuls` hook (C20066EC). Only noA removed the fmuls hook, but noA also ran double-speed. So the fmuls hook was never tested in isolation at correct speed.

**Round-2 result (user):** M worked in noA ONLY; broken in noB/noC/noFmuls. The ONLY difference: noA leaves `0x804167B8` at stock **0.5**; all others set **2.0**. EmulationSpeed=2.0 is on in noA too, so it's NOT the emulator speed.

**⚠️⚠️ MAJOR CORRECTION (2026-07-30, later): the sqrt story below is WRONG. It used a bad SDA base.** The real `r2 = 0x80416ba0`, `r13 = 0x804141c0` (from `__init_registers` @ 0x8000536C), NOT the 0x8041E790 I'd back-derived. Redone with correct r2: **`0x804167B8` (stock 0.5, mod writes 2.0) is a legitimate FRAME-RATE value** read by exactly 3 animation/timing sites: `0x802A5EFC` (func 0x802A5B44, narrow single-caller chain, computes `framerate*(600.0-t)→int` via fctiwz), `0x802A7C24` (func 0x802A7BD8, **215 callers**, the general anim-rate converter `const/(x*framerate)`), `0x802A7C94` (func 0x802A7C48, 14 callers). The sqrt 0.5 constant is a DIFFERENT address `0x8040EBC8` (the gecko fmuls hook reads THAT, and the mod never touches it). **So the mod does NOT corrupt sqrt. TEST B is a real 120fps framerate hack, which is why it's genuinely butter-smooth.** The M breaks because framerate=2.0 makes one of those 3 timing readers produce a bad (2x) frame count for the gate; clean targeted fix likely. Bisection: patch each reader's `lfs ...,-0x3e8(r2)` → `-0x7fd8(r2)` (reads 0.5 from 0x8040EBC8) while global stays 2.0: `$FIX R1 042A5EFC C0428028`, `$FIX R2 042A7C24 C0028028`, `$FIX R3 042A7C94 C0028028` (each = noB + one patch). Whichever restores the M while keeping speed = the fix (R1 narrow = cleanest; R2 general = would slow all anims). Also correct [[sunshine-simrate-mechanism]] which has the same wrong-r2 sqrt claim.

**$FIX R1/R2/R3 RESULTS (user):** R1 (patch reader1)=smooth 120 but M still broken; R2 (patch reader2)=same; **R3 (patch reader3 @0x802A7C94 → read 0.5) = M WORKS + 4x speed.** So **reader3's function `0x802A7C48` is the M's dependency**: it's the shared anim-rate accessor: returns `tvModeConst(-0x3c8/-0x3c4(r2), NTSC/PAL) * framerate(0x804167B8)`, gated on TV mode from `bl 0x8035069c`. At framerate 2.0 the M's glow BCK advances 2x and overshoots into the OFF frame. R3 fixes M but forcing the accessor to 0.5 breaks all 14 callers' timing (=4x speed). **Clean fix = make ONLY the gate's animation use stock rate.** BLOCKER: `0x802A7C48`'s 14 callers are shared anim methods (sizes 0x28-0x898; strings seen: "<TDirector>", "ルートフェーダー"/route-fader, progressive-scan prompt. NONE is TModelGate::perform (JP 0xAA8), so the gate calls anim-rate INDIRECTLY through a shared J3D/MActor update). Need to locate TModelGate::perform / the gate's glow-anim setRate in the unmapped USA binary (via its story-flag reads 0x10386 etc., or the glow material-anim among the 14) and patch its rate to stock. Next step, not yet done.

**[OBSOLETE / WRONG below, kept for history] ROOT CAUSE (disasm, WRONG r2):** `0x804167B8` = the **0.5 Newton coefficient of the inlined square-root routine** (3 readers, all `frsqrte`+`fnmsubs` rsqrt; 0 timing readers, full dataflow scan). Mod's `044167B8 40000000` corrupts those sqrts, so the M portal's distance-based proximity glow computes garbage and never lights. The mod **mislabeled a math constant as a framerate scalar**; its "speed control" is a side effect of sqrt corruption halving vector magnitudes. **You cannot get correct-sqrt (working M) AND value=2.0 speed from this global.** Same bytes. Corrected [[sunshine-simrate-mechanism]].

**Timing architecture trace (2026-07-30, USA main.dol):** The true frame-timing path (NOT the sqrt constant):
- VI-pacing fn real entry `0x802FC9A4` (body `0x802FC9AC`; `mflr` at 9A4, not the `stwu` at 9AC; watch fstart). Waits on `0x8034f684` (OSDisableInterrupts+sleep-until-retrace-count-advances) until `retraceCount` reaches `target = prevTarget + subStepCount`. The mod's `042FCB24` NOPs one of its 4 waits.
- Called ONCE from `0x802F80EC` inside the display/present virtual method `0x802F80D0` as `pace(this->0x60 /*render target*/, this->0x4C /*u16 subStepCount*/)`. That method then does the render (`0x802ca1e0`,`0x802f917c`), GX flush (`0x8035d8f0`), and toggles double-buffer index `this->0xC ^= 1`. Its vtable is at `0x803E1D..` (slot `0x803E1DCC`); method cluster `0x802F7D28..0x802F8294` = the frame/display class.
- **subStepCount (`0x4C`) = how many 60Hz VI fields to wait per presented frame** (stock 2 → 30fps). It gates PRESENTATION, and Sunshine runs exactly 1 logic update per present (fixed-timestep, logic⇄present coupled 1:1).

**VERDICT: no clean single-knob timestep scale exists.** Because logic⇄present are 1:1, changing subStepCount or EmulationSpeed changes fps but scales logic-updates-per-real-second identically → game speed always tracks fps. The only independent lever would be dt-per-update, which Sunshine bakes as 1/frame pervasively (no knob, matches [[sunshine-simrate-mechanism]] "integer-quantized"). That is *why* the mod corrupted the sqrt constant: a crude global "shrink all motion" to fake a smaller dt, which is exactly what breaks the M's distance check. Proof from user's own tests: correct sqrt (0.5) ⇒ always 2x speed; the ONLY way they got correct speed was corrupting sqrt. **Correct-speed + high-fps + correct-math is unreachable via game-logic scaling; requires render-side frame interpolation in the Dolphin build (decouple present from logic OUTSIDE the game). The pacing point `0x802FC9A4`/present `0x802F80D0` is the natural place for Dolphin to know a frame is presented.**

**`$TEST decouple` RESULT (user, confirms the tradeoff is ONE knob):** sqrt left correct (0.5) + fmuls hook pointed at a separate real 2.0 → **double speed + 12-28fps (unplayable)**. So the speed correction in TEST B comes from the **sqrt corruption itself** (velocity magnitudes scaled), NOT the fmuls hook. Therefore correct-sqrt (working M) and correct-speed are inseparable at the global level; no Gecko/config combo gives both. Frame interpolation also rejected by user: reintroduces input latency TEST B avoids (30fps logic), and blend v1 looked worse/choppier. **User strongly prefers TEST B feel; wants the M fixed without losing it.**

**Only remaining path = targeted patch so the M portal ignores the corrupted sqrt while everything else stays corrupted.** Decomp evidence it's feasible: `TMario::warpIn` (MarioAutodemo.cpp) uses `mWarpInDir.normalize()` (rsqrt) + Mario-to-gate distance; `TModelGate::perform` (JP 0x801C2A5C, 0xAA8 bytes) does the proximity glow. Hypothesis: the gate's proximity/enterability check reads a corrupted distance → reads "too far" → never glows/enters even when Mario is on it. Fix = patch that specific distance check to be correct (Gecko or Dolphin binary patch). BLOCKER: no USA symbol map, so locating `TModelGate::perform`/proximity-check in USA `main.dol` is the hard part (JP addresses ≠ USA). Not yet attempted.

**⭐ BLOCKER SOLVED (2026-07-30, later agent): TModelGate located in USA `main.dol`.** The ModelGate.cpp TU is at USA **0x801EAC64–0x801EC8C0**, method sizes matching JP EXACTLY: **perform__10TModelGate = 0x801EB014** (0xAA8), receiveMessage = 0x801EBBDC (0x1A8), screenBlur = 0x801EBD84 (0x250), startOpen = 0x801EBFD4 (0x74), loadAfter = 0x801EC048 (0x878), getTakingMtx = 0x80007D70 (shared 0x8). Verified: perform calls screenBlur @0x801EBA90. Found via ROBUST function-boundary detection (all `mflr r0` + bl-targets); naive "mflr THEN stw r0" prologue detection misses `mflr;lis;stw` forms and MERGES functions, corrupting every earlier size-based guess (incl. the wrong 0x8029ba68/0x8029c520 candidates). Scripts: /tmp/sms_starts.py (robust starts), /tmp/sms_window2.py (TU size-multiset fingerprint), /tmp/perf2.txt (full perform disasm).

**Gate glow mechanism (fully reversed):** `this` fields: 0xd0=glow(f32,0..1), 0xc8=timer-seed(s16=360 set in loadAfter), 0xca=countdown(s16), 0xc4=substate(u8), 0x78=MActor, 0x72=bone idx. loadAfter bakes ALL rates as FIXED CONSTANTS (0xd4=0.1 touch-rise, 0xd8=0.02/0xdc=0.025 fades, rise 0.01=[0x80414904]); receiveMessage does `glow+=0.1` per frame Mario in range (dist²<40000 → radius 200) then 0xca=0xc8 when glow hits 1. perform payoff @0x801EBA60-7C: `f2=glow; N=(s16)getAnm(MActor,5)->8; f31=glow*N; getAnm(MActor,5)->0x10=f31`: a DIRECT FRAME-SCRUB of anim-slot-5 (glow×maxframe). **KEY FINDING: the gate glow LOGIC never reads reader3/framerate. Pure fixed-constant + frame-counter.** So reader3 acts via the SHARED J3D anim calc advancing the gate's MActor model anims (created at default rate by MActor factory 0x8023E5C4 in loadAfter; NO per-gate rate store exists). getAnm=0x80238F08 (`r3=MActor->0x28[idx]; return r3+4`); slot raw obj O: 0xC=s16 maxframe, 0x14=f32 curframe. reader3's 14 callers are NOT the MActor-array advance (they're effect-ctors baking reader3 as rate [singletons @0x802A6398], directors, script timers), consistent w/ R2 not fixing M while R3 does.

**⭐⭐ READER3 IDENTIFIED + CALLERS NAMED (2026-07-31, via decomp; reader3 = `SMSGetVSyncTimesPerSec()`,** `Application.cpp:76`: (60|50 by TV format)/2 = **30** stock; USA global 0x804167B8 is the ÷2 factor (0.5), hack makes it 120. reader2 = `SMSGetAnmFrameRate()` = 60/vsync (2.0 stock, 0.5 hacked). Named callers (USA callsite → decomp): 0x80299850 → **`MarDirectorDirect.cpp:44` `int vsyncRate = 600/(int)vsync`** (main-loop substep budget: `unk54 += vsyncRate`, −5 per substep, flags 0x2000=first/0x4000=last substep; stock 4 substeps/frame, hacked 1; **this caller is the 4-8x speed lever**); 0x802963C0 → **`Application.cpp:241` route fader `mFader = new TSmplFader(black, vsync, "ルートフェーダー")`**, rate **baked once at boot**; 0x802957A0/8A8 → GCLogoDir boot logo (`unk44/vsync > 1.0`); 0x80298578 → `fadeOutAllSound(vsync*0.4)`; 0x801727E4 → `getWipeCloseTime()=30/vsync` (ConsoleStr); RumbleMgr/MenuDir/ProgSelect = others. Gate-open path: stage SPC script → EventWatcher binding `startOpenModelGate` (`EventWatcher.cpp:404` evStartOpenModelGate → `gate->startOpen()`); TEventWatcher::perform just runs `mInterp->update()` on CUE_MOVE.

**C2-code gotchas discovered (cost several void test rounds):** (1) Dolphin's C2 handler OVERWRITES the last word of the block with its branch-back. Block MUST end `00000000` pad or the last instruction is clobbered (caused Invalid-write crash). (2) the C2 cave is small; many/large C2 codes silently don't run (Diag-Rest/Sched/All14 results were all VOID). Both documented in repo skill `.claude/skills/dolphin-gecko/SKILL.md` + helper `scripts/gecko.py` (also handles: Dolphin rewrites user GameSettings ini from memory on quit; edits while running are clobbered).

**Valid bisection results (LR-conditional single C2 at reader3 0x802A7C94 reading caller ret addr from 0x14(r1)):** low-7 callers→0.5: M broken, normal speed. Group {958AC GCLogo, 963C4 fader-ctor, 9857C sndfade, 99854 MarDirector}→0.5: **M WORKS, 4-8x speed** (speed from 99854). Current test: `$120fps + M-fix Fader` = noB + single 04 patch **`042963C0 C022B768`** (replaces fader-ctor's `bl vsync` with `lfs f1,-0x4898(r2)` = 30.0 @0x80412308); fader baked 30, zero speed impact. If M works → SHIPPING FIX. If not, lever is MarDirector vsyncRate line and fix must target substep structure (unk2=3 both-flags-same-substep / uVar8 differences at 1 substep).

**⭐⭐⭐ ROOT MECHANISM FOUND (2026-07-31): rise/decay clock mismatch.** Gate glow = balance of: RISE `glow += 0xd4(=0.1)` per receiveMessage, driven by `gpObjHitCheck->checkActorsHit()` which `direct()` gates on `!(unk58 & 3)` → **30/sec REAL-TIME regardless of fps** (unk58 += 1/substep ≈ 120/s always) → rise = +3.0/s; DECAY `glow -= 0xd8(0.02)/0xdc(0.025)` in TModelGate::perform → **per rendered frame** → 30fps: −0.75/s, 120fps: −3.0/s = **exact cancellation → M permanently dark at 120**. Explains all tests: Bisect-2/R3 "fixed" M because vsyncRate=20 → 4 substeps/frame → hit-check every FRAME → rise 12/s ≫ decay (and 4 substeps = the 4x speed); fader/GCLogo/sndfade were passengers (fader-only test: M still broken). Also: FIX R2 not fixing M is consistent (anim rates irrelevant); EmitterViewObj `(int)SMSGetAnmFrameRate()=0`-loop hypothesis rejected (would break all particles, only M is broken).
**SHIPPED SURGICAL FIX: `$120fps + M-fix Glow` = noB base + `041EC294 C002B830`**: patches TModelGate::loadAfter's init of per-gate rise field 0xd4 (`lfs f0,-0x2240(r2)`=0.1 @0x80414960 → `lfs f0,-0x47d0(r2)`=0.4 @0x804123D0) → rise 12/s vs decay 3/s at 120fps → glow fills ~0.15s. Gate-scoped (only TModelGate reads 0xd4), zero speed impact, single 04 word. Note post-glow lit-window 0xca=360 frames = 3s at 120fps (12s stock); still plenty to enter. AWAITING user confirm.

**⭐⭐⭐⭐ MASTER SWITCH FOUND (2026-07-31 late): TModelGate::perform's FIRST check is `if (!(0x70 & 1)) skip entire body` (0x801EB034-3C beq→0x801EBAA4).** Bit 0x70&1 is set ONLY by startOpen() (0x801EBFD4), which loadAfter does NOT call (verified: tail has no bl to it; the 0x803EC160+0xa0 check block just wires 3 ripple-layer ptrs). Gates start CLOSED every scene load; only the stage SPC script (evStartOpenModelGate) opens them. ALL prior constant-tuning (rise 0xd4, decay, hit-cadence, cue bits) was downstream of a switch never flipped. Explains every failed test. receiveMessage glow needs 0x70&2 (also startOpen-only); the take/warp branch (msg 0xe, sender type 0x80000001) has NO glow/flag check visible.
**Misidentification fixed:** fn 0x80296330 (reader3 LR 0x802963C4) = **TGCLogoDir ctor** (0x48-byte obj from director-factory jumptable fn 0x802a6398, big method 0x80295c10), NOT the route fader; 0x802a41bc (LR 0x802A4254) = TMenuDir ctor (0x58 obj). The real TSmplFader ctor is likely fn 0x8017dd80 (stores rate→this+0x12C, LR 0x8017DDF8, was in Bisect-1 low group). **Boot-race caveat: Gecko codehandler runs on VI hook. Boot-time ctors execute BEFORE first application in every test, so all boot-baked rates were never actually altered by any bisect; bisect discrimination only ever affected callsites running during gameplay.** So Bisect-2's in-game-effective set ≈ {MarDirector vsyncRate, sndfade-fn 0x80298250} only.
**Current probe: `$120fps + PROBE ForceOpen`** = noB + C2 @0x801EB034 (7 lines): guarded (skip if 0x70&1 already set) absolute call to real startOpen(0x801EBFD4) per gate perform, replicates script opening exactly (anim switch + flags + state). If M works at normal speed → root cause = script/EventWatcher never fires startOpen at 120fps-1-substep; probe may even serve as practical fix (only placed gates exist per scenario). Next purity step would be extracting dolpic .sb script from RVZ to find its stalled wait. If M still dark → perform not even called / deeper.

**⭐⭐⭐⭐⭐ WORKING SOLUTION FOUND (2026-07-31): zone-scoped auto-0.5.** PROBE SpeedIfGateTicks (C2 @perform-entry 0x801EB034 writing 0.5→0x804167B8 every gate tick) revealed: (1) gate perform RUNS at 120fps ✓; (2) with framerate 0.5 in-Delfino-only, **M works fully** at 4x Delfino speed while levels/cutscenes stay prime 120 (04-code re-writes 2.0 per VI; retrace-NOP means many frames/VI so gate's write dominates in-plaza). ForceOpen (real startOpen call) did NOT fix M → the script needs 0.5 for more than startOpen; exact reader still unidentified (remaining suspects: 4-substep structure side effects; changeState×4; NOT anim rates/hit cadence/cue bits/glow constants, all tested). User reaction: "WOW OK". **Shipped `$120fps + M AUTO (slow only within 200u of a portal)`** = noB + C2 @0x801EB034 (10 lines): computes XZ dist²(pos-vec@-0x60b4(r13) vs gate 0x10/0x18) < 40000 (stock glow radius 200u = pool const -0x2270(r2)) → write 0.5; else leave 2.0 (VI 04-write restores ≤1/60s after leaving). Caveat: if gate-open script wait is a poll it completes while standing near (expected); if one-shot-missed, fall back to whole-plaza probe version (proven). fcmpu enc note: FC0D0800 (fcmpo=+0x40; capstone won't decode fcmpo). Truly-surgical fix (normal speed everywhere + M) would need the stage .sb script extracted from RVZ to find the 0.5-dependent wait; optional purity work.

**⭐⭐⭐⭐⭐⭐ SPLASH/EFFECT-ANIM BUG CONFIRMED + FIXED (2026-07-31): EmitterViewObj int-truncation.** User observed small splash/effect anims MISSING at 120fps, present in 0.5 mode → exactly `EmitterViewObj.cpp` `for (int i = SMSGetAnmFrameRate(); i > 0; --i)`: hacked (int)(60/120)=0 → particle-emitter step loop runs ZERO times. reader2 (0x802A7BD8) IS SMSGetAnmFrameRate (vsync inlined into it, same TU; hence "const/(x*framerate)" shape). USA truncation sites (bl reader2 → fctiwz f0,f1): **0x802887A8, 0x80288D30, 0x80288DEC** (EmitterViewObj's 3 loops, same TU as particle mgr 0x80287fdc; 4th scan hit 0x80097CD4 = false positive across branch). FIX = C2 at each fctiwz: `f1 += 0.5` before truncation (lfs f0,-0x7fd8(r2); fadds f1,f1,f0 = C0028028/EC21002A); stock 2.0+0.5→2 unchanged, PAL 2.4+0.5→2 unchanged, hacked 0.5+0.5→1 (particles sim 1×/frame = 2× evolution speed, minor). Shipped codes: `$120fps + FX`, `$120fps + M AUTO + FX` (new benchmark), `$120fps + FX + ForceOpen` (TRUE-FIX candidate: if M-broken decomposes as invisible-glow-particles + already-enterable, this gives M at full 120/normal speed; user must JUMP INTO the M even if dim). Cave note: 25-line code near limit; if FX inert (splashes still missing) suspect overflow, split codes.
**HD texture pack installed (2026-07-31): "SMS 4K 2.0c (1080p)"** → `~/Library/Application Support/Dolphin/Load/Textures/GMS/` (region-free GMS prefix, 757MB); GFX.ini: HiresTextures=True + CacheHiresTextures=True (backup GFX.ini.bak). 4K variant still in ~/Downloads if wanted. User feedback on M AUTO: works, benchmark; 4x near portal makes it easy to overshoot. Radius tweak (200u→400u via extra ×0.25 on dist², or smaller) available if asked.

**USER-CONFIRMED (2026-07-31 eve): `M AUTO + FX` = working benchmark. Particles/splash FX restored ✓, M works via proximity-0.5 ✓. FX+ForceOpen did NOT give a true fix (M needs 0.5 for more than startOpen).** New user observations: (1) **when broken there's a BIG SOLID HITBOX blocking the portal space; it disappears when the M lights.** The closed gate has a physical barrier. loadAfter sets `0x64 |= 1` (801ec0a4), startOpen CLEARS bit0, so 0x64 bit0 is likely the barrier/solid flag, but clearing it via ForceOpen was insufficient. Barrier likely = the gate's map-collision mesh (0xc0 obj, ctor 0x8023ef2c(new0x20, modeldata)) which must follow the OPEN anim; why that fails at framerate=2.0 still unknown (TMapCollisionMove::move() itself is clean float code). (2) **whirl effects linger after walking away**: whirl runs in gate state 0xc4>=2 (perform 801eb540+: phase counter 0xb9 0..7 advancing every 0xbc ticks reload 0xbe=240, GX-material byte writes via jumptable @0x803D4050, layer copy via bitfield helpers); its wind-down only works at 0.5, froze when Mario left the proximity zone.
**Shipped `$120fps + M AUTO v2 + FX` (27 lines):** radius 400u (dist²×0.25 via two fmuls by 0.5 before compare vs 40000) fixes the 4x-overshoot complaint + **state-hold**: gate 0xc4>=2 also forces 0.5 regardless of distance (whirl sequences finish). C2 block layout at 0x801EB034 documented in /tmp/build_auto2.py (regenerate: encodings verified per-word w/ capstone; fcmpu FC0D0800 capstone-undecodable, decode manually).

**TEST-MATRIX HOLE FOUND (2026-07-31 night): glow-boost and ForceOpen were never combined.** Glow rise/decay tie at 120 (−3.0 vs +3.0/s) needs BOTH the rise-boost (041EC294 C002B830: 0xd4 0.1→0.4) AND ForceOpen (gates open; script dead at 2.0) AND FX (emitter loops; glow particles visible). Each alone was doomed: rise-boost tested with gates closed (moot), ForceOpen tested with tied glow (stuck at 0). J3DFrameCtrl::update (J3DAnimation.cpp:142) semantics: mState bits 1/2 = COMPLETED/LOOPED-once. State0's slot-2 wait completes at rate 0.5, just slower, NOT a stall. **Shipped `$120fps + TRUE-FIX candidate` (23 lines) = noB + rise-boost + FX + ForceOpen at FULL speed. Instruct user: stand at M ~1s, watch glow fill, jump in.** If works → true fix achieved (residuals: whirl phases 4x fast cosmetic, lit-window 3s, glow snappier). If M lights but barrier still blocks → barrier is collision-mesh-follows-anim, dig 0x8023ef2c/TMapCollisionMove wiring next. If no light → decay path is something else; instrument glow value.

**⭐⭐⭐⭐⭐⭐⭐ GLOWPIN RESULT (2026-07-31 night): Ms CONSTANTLY LIT at full 120fps normal speed ("WOW!!")** Forcing glow=1.0 + 0xca=0xc8 inside perform (C2 @0x801EBA60) lights everything, confirming the **entire OUTPUT pipeline (glow→slot5 scrub→J3D→pixels + whirl) works at 2.0 timing.** Only dead link = INPUT: receiveMessage proximity path (Mario hit-messages → dist checks → glow rise) never fires at 2.0. Root cause still unknown (suspect: hit-pair enumeration or the bone-mtx-based dist calc 0x8034a2d0(this+0x7c, sender+0x10) with stale anm-mtx). Entry-through-lit-M not yet confirmed by user (asked).
**Also: cave-overflow theory DEBUNKED.** The multi-C2 failures (Diag-Rest/Sched/All14) were ALL the last-word-clobber bug (their blocks ended in real instructions); Dolphin's Gecko buffer is ~0x700 bytes ≈ 200+ lines. 33-line codes fine.
**Shipped `$120fps + TRUE-FIX v2 (proximity glow reimplemented)` (33 lines)** = noB + FX + ForceOpen + C2 @0x801EBA60: computes XZ dist²(posvec@-0x60b4(r13) vs gate 0x10/0x18) < 40000 (stock 200u radius) → glow=1.0 + pin 0xca; else re-exec original lfs (natural decay fades it). Live regs at hook: r0 (0x4330 magic) and f1 (176.0 dbl), MUST NOT clobber; used r3/r4/r11/r12/f0/f2/f13. Encodings: fsubs f2,f2,f0=EC420028, fmuls f13,f2,f2=EDA200B2, fmadds f13,f2,f2,f13=EDA268BA, fcmpu cr0,f13,f0=FC0D0000. If entry works → THIS IS THE TRUE FIX (stock-like proximity glow, no speed changes). Remaining cosmetics: whirl phase 4x fast, lit-window 3s, decay 4x fast.

**✅✅ SOLVED - USER CONFIRMED (2026-07-31): "it works amazingly by the way, i'm stunned!!"** TRUE-FIX v2 = the shipping solution: M portals light on approach + enterable at FULL 120fps normal speed everywhere. User requested radius 200→350; **replaced in-place with ~354u version** (threshold built from pool floats: 500²×0.5=125000, consts [0x8041496C]=500 @-0x2234(r2), 0.5 @-0x7fd8(r2); +2 words fmuls f0,f0,f0=EC000032 / fmuls f0,f0,f2=EC0000B2; 35 lines total). Final architecture of `$120fps + TRUE-FIX v2`: (1) noB 120fps base; (2) FX = 3× C2 `f1+=0.5` before fctiwz at 0x802887A8/80288D30/80288DEC (EmitterViewObj zero-loop fix, also restores ALL small particle FX game-wide); (3) ForceOpen = C2 @0x801EB014-entry(0x801EB034) guarded call to real startOpen 0x801EBFD4 (replaces the script's job; script still dead at 2.0, root of THAT never found, acceptable); (4) proximity-glow reimpl = C2 @0x801EBA60 XZ-dist² vs ~354u → glow=1.0+pin 0xca, else original (natural fade). Daily-driver alternates kept in ini: M AUTO v2+FX (slow-zone), GlowPin (always-lit). Remaining knowable-but-unfixed: WHY receiveMessage proximity input + SPC script die at framerate 2.0 (academic now). Cosmetic deltas at 120: whirl phases 4x, lit-window 3s, fast fade.

**SEQUENCE-BREAK BUG + FIX (2026-07-31): user found the LOCKED Gelato M visible/lit despite goop.** ForceOpen's fault (opens ALL placed gates, ignoring story flags the SPC script checks). Evidence re-audit: "script dead at 2.0" was NEVER isolated; all its supporting tests also had the glow-input dead; the script may well open the correct gates at load even at 2.0 (AUTO-mode "lights when near" is fully explained by receiveMessage-input-dead + near-0.5 reviving it). **Shipped `$120fps + TRUE-FIX v3 (respects story locks, no ForceOpen)` (27 lines) = noB + FX + proximity-glow(354u) ONLY.** If v3's unlocked gates still light on approach, script was alive all along and v3 is FINAL (story-correct: goop-locked gates stay closed). If v3 gates never light, script really is dead at 2.0; fallback design ready: per-gate timeboxed "script window" (C2 @0x801EB034: while gate closed AND gate->0xcc<180: 0xcc++, write 0.5; loadAfter zeroes 0xcc each scene, locked gates stop forcing after 1.5s so no permanent 4x; window covers scene fade-in where 0.5 is invisible; script polls and opens flag-correct gates during it).

**Gate-only test Geckos handed to user (C2 hook at frame-scrub; CANNOT cause global 4x-speed):** ×0.5 = `C21EBA74 00000002 / EFE20032 C0028028 / EFFF0032 60000000`; ×0.25 = `C21EBA74 00000002 / EFE20032 C0028028 / EFFF0032 EFFF0032`. (C0028028 = `lfs f0,-0x7fd8(r2)`=0.5 @0x8040EBC8; layer on noB base.) Diagnostic: M lights→slot-5 WAS the overshoot culprit (done); M just dims→0x14 is a directly-rendered frame & the overshoot is a different free-running controller (next: inject per-frame rate override on gate controllers, or Dolphin binary patch). Awaiting user in-game result.

**PRACTICAL SOLUTION shipped (2026-07-30):** hold-to-enter Gecko code in GMSE01.ini `$120fps + HOLD Dpad-Left to enter M portals` = noB base + button-activated override `28404454 FFFE0001 / 044167B8 3F000000 / E2000001 80008000` (button addr 0x80404454 for GMSE01 controller-1, from gamemasterplc's Jesus Mode code; `28[addr] [~bit][bit]` = while-held). Hold D-pad Left → framerate value → 0.5 → M works (game runs decouple-speed while held, fine for the ~2s to walk in); release → back to prime 120fps. No reboots. R3-config surgical fix (make ONLY gate's glow use stock rate) handed off to another agent.

**Consequences / fix paths:**
- *Immediate workaround:* to enter any rainbow-M stage portal, switch to `$TEST noA` (or native 30fps). M glows/works there (runs 2x speed but that's fine for just walking into the portal). Switch back to `$TEST noB` for playing levels (levels have no M portals). Save file is healthy.
- *Proper fix (real project):* framerate must NOT ride on the sqrt constant. Options: (a) binary-patch the custom Dolphin build to scale the director sub-step count passed into pacing loop `0x802FC9AC` (the true timing knob), like the audio fix; (b) frame interpolation (render-only, no game-logic change), matching [[sunshine-highfps-hardware-ceiling]] "needs interpolation."
- `$TEST decouple` added to the ini (sqrt stays 0.5; injected fmuls hook re-pointed at a real 2.0 constant at `0x80416808` = `C2C28078`). Predicted: M works but ~2x speed (confirms speed rides on corruption). Not a real fix; diagnostic only.

See [[sunshine-simrate-mechanism]], [[sunshine-audio-fix]], [[sunshine-highfps-hardware-ceiling]]. Gate logic (`TModelGate::perform`) is NOT yet in the decomp (`ModelGate.cpp` is an empty stub); would need USA-address disasm to fully reverse the glow code.
